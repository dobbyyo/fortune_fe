interface BaseNavBarProps {
  title: string;
}

const BaseNavBar = ({ title }: BaseNavBarProps) => {
  return (
    <div className="navbar w-full flex items-center px-4 relative">
      <div>asd</div>
    </div>
  );
};

export default BaseNavBar;
